{
    "name": "up-baloes/sistema",
    "description": "Sistema de gerenciamento de decorações com balões - Up.Baloes",
    "type": "project",
    "require": {
        "php": ">=7.4",
        "firebase/php-jwt": "^6.11",
        "vlucas/phpdotenv": "^5.6"
    },
    "autoload": {
        "psr-4": {
            "UpBaloes\\": "src/"
        }
    },
    "config": {
        "optimize-autoloader": true,
        "preferred-install": "dist",
        "sort-packages": true
    },
    "minimum-stability": "stable",
    "prefer-stable": true,
    "scripts": {
        "post-install-cmd": [
            "@php -r \"if (!file_exists('.env') && file_exists('.env.example')) { copy('.env.example', '.env'); echo 'Arquivo .env criado a partir do .env.example\\n'; }\""
        ]
    }
}


